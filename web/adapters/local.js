/* =====================================================================
   Хранилище: только этот браузер
   ---------------------------------------------------------------------
   Ничего никуда не отправляет. Существует ради двух случаев: попробовать
   приложение, не заводя аккаунтов, и не потерять записи, если хранилище
   ещё не выбрано.

   Про риск говорим прямо в интерфейсе: Safari чистит данные сайтов, и
   тогда дневник исчезнет вместе с ними. Поэтому единственная защита
   здесь — регулярно скачивать копию файлом.
   ===================================================================== */

export default {
  id: 'local',
  name: 'Только этот браузер',
  tagline: 'Без аккаунтов и настройки. Но дневник живёт только на этом телефоне.',
  needsNetwork: false,
  warn: 'Safari периодически чистит данные сайтов. Скачивай копию раз в пару недель — иначе однажды дневник пропадёт.',

  fields: [],
  help: ['Ничего настраивать не нужно. Дневник хранится в памяти браузера на этом устройстве.'],

  async check() { return { ok: true }; },
  async load() { return { doc: null, rev: null }; },
  async save() { return { rev: null }; }
};
